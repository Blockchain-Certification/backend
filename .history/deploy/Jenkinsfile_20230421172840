pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                sshagent(credentials: ['vinhphan-root-keys']) {
                    sh 'ssh -o HostKeyChecking=no -l root 194 "command"'
                }
            }
        }
    }
}

